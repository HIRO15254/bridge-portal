{
  "name": "@bridge-portal/server",
  "private": true,
  "type": "module",
  "main": "src/worker.ts",
  "scripts": {
    "build": "wrangler deploy --dry-run",
    "check-types": "tsc --noEmit",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@bridge-portal/api": "workspace:*",
    "@bridge-portal/db": "workspace:*",
    "@hono/trpc-server": "^0.4.2",
    "@trpc/server": "catalog:",
    "hono": "catalog:"
  },
  "devDependencies": {
    "@bridge-portal/config": "workspace:*",
    "typescript": "catalog:",
    "vitest": "4.1.2",
    "wrangler": "4.80.0"
  }
}
